name: Example WF

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest          # ← This is GitHub-hosted (free for public repos)

    steps:
      - name: Hello Bharath
        run: echo("Hello Bharath")

      - name: List Files
        run: ls

      - name: Read the file
        run: cat Dockerfile
