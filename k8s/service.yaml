apiVersion: v1
kind: Service
metadata:
  name: sample-python-service
spec:
  type: NodePort
  selector:
    app: sample-python
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30005                       # Pick any free port > 30000
